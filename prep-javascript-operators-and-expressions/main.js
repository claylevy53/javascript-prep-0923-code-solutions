const number1 = 5;
const number2 = 3;

const product = number1 * number2;

console.log(product);
console.log('This data type is a:', typeof product);

const payment = 100;
const charge = 200;

const amountRemaining = charge - payment;

console.log(amountRemaining);
console.log('This data type is a:', typeof amountRemaining);

const tests = 1;
const assignments = 2;
const final = 3;

const grade = (tests + assignments + final) / 3;
console.log(grade);
console.log('This data type is a:', typeof grade);

const firstName = 'Clay';
const lastName = 'Levy';
const fullName = firstName + ' ' + lastName;

console.log(fullName);
console.log('This data type is a:', typeof fullName);

const pH = 8;
const isAcidic = pH < 7;
console.log(isAcidic);
console.log('This data type is a:', typeof isAcidic);

const numberOfSoldiers = 301;
const isSparta = numberOfSoldiers === 300;
console.log(isSparta);
console.log('This data type is a:', typeof isSparta);

const nickname = fullName + ' ' + 'is the GOAT';
console.log(nickname);
console.log('This data type is a:', typeof nickname);
